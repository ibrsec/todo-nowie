import React from 'react'



export const metadata = {
    title:"Login | Todo Nowie",
    description:"Login page of Todo Nowie", 
  }

  

const LoginLayout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default LoginLayout